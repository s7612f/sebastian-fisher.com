# Copy this to .env and fill in values before running docker compose
# DO NOT commit .env to git — it contains secrets

# Umami analytics
UMAMI_DB_PASSWORD=change_me_strong_password
UMAMI_APP_SECRET=change_me_random_32_char_string

# Generate UMAMI_APP_SECRET with:
# openssl rand -hex 32
